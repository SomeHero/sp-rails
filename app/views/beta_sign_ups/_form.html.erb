<%= form_for(@beta_sign_up) do |f| %>
  <% if @beta_sign_up.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beta_sign_up.errors.count, "error") %> prohibited this beta_sign_up from being saved:</h2>

      <ul>
      <% @beta_sign_up.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :emailAddress %><br />
    <%= f.text_field :emailAddress %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
